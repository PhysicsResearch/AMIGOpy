# Export


Go to Export > Type > Numpy

A window will open to select the foldera npy file called AMBpy_export.npy containing the images being visualized will be saved to that folder.

- this function is a quick fix to allow data export and will be improved in the future.

- rename the file after exporting